The JAR contained in this directory is a custom build of [keycloak-config-cli](https://github.com/adorsys/keycloak-config-cli/releases/tag/v6.4.1).

It is required for the first broker login flow configuration to work since `keycloak-config-cli` seems to have issues with it at the moment.